- Create database and set `DATABASE_URL` in `.env` file
- Sync database with `npm run sync`
- Populate database with `npm run populate`

## Results
typeorm: ~3.7s
prisma query: ~6.1s
prisma join: ~1min 26s
